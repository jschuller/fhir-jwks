# fhir-jwks

Public JSON Web Key Set for a personal, read-only Epic patient-access FHIR app
(SMART App Launch, asymmetric client authentication). It contains public keys
only. The private key never leaves the machine that generated it.

Served at `https://jschuller.github.io/fhir-jwks/jwks.json`.
